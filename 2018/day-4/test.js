'use strict';

const assert = require('assert');
const solution = require('./solution');

describe('', function() {
  before(function() {});

  it('', function() {
    assert.equal(true, true);
  });
});
